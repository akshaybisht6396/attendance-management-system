# Set the standard port for the Eureka server
server.port=8761

# These two lines tell the server NOT to register with itself
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false